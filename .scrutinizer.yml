imports:
    - php

tools:
    external_code_coverage:
        timeout: 2100

filter:
    excluded_paths:
        - SEOstats/Services/3rdparty/*
        - vendor/*
        - tests/*
        - app/*
        - bin/*
